name: Verify desktop package matrix

on:
  workflow_call:
    outputs:
      windows-artifact-name:
        description: Artifact containing the verified Windows package and unpacked bundle
        value: ${{ jobs.packaged-native-boundary.outputs.artifact-name }}
  workflow_dispatch:

permissions:
  contents: read

jobs:
  packaged-native-boundary:
    timeout-minutes: 60
    name: ${{ matrix.platform }}-${{ matrix.arch }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-2022
            platform: win
            arch: x64
            builder_flag: --win
    runs-on: ${{ matrix.os }}
    outputs:
      artifact-name: ${{ steps.artifact.outputs.name }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Verify September repository destination
        run: node scripts/check-github-destination.mjs

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Read package version
        id: version
        shell: bash
        run: echo "version=$(node -p "require('./package.json').version")" >> "$GITHUB_OUTPUT"

      - name: Verify target matrix and protected-claim gate
        shell: bash
        run: |
          node --test scripts/packaged-targets.test.mjs scripts/check-protected-vault-claims.test.mjs scripts/check-packaged-vault-lock-native.test.mjs
          node scripts/check-protected-vault-claims.mjs
          node scripts/check-packaged-freshness-guard.js

      - name: Build packaged renderer and native worker
        shell: bash
        run: |
          npx vite build --config vite.config.electron.ts
          node scripts/build-native-engine.mjs

      - name: Build target package
        shell: bash
        run: >-
          npx electron-builder
          --config electron-builder.json
          ${{ matrix.builder_flag }}
          --${{ matrix.arch }}
          --publish never

      - name: Verify packaged native module under shipping Electron ABI
        shell: bash
        env:
          KYUTXO_PACKAGED_SKIP_BUILD: '1'
        run: >-
          node scripts/check-packaged-native-engine.mjs
          --platform ${{ matrix.platform }}
          --arch ${{ matrix.arch }}
          --require-electron

      - name: Verify package filename and installed app version
        shell: bash
        run: >-
          node scripts/check-packaged-app-version.mjs
          --platform ${{ matrix.platform }}
          --arch ${{ matrix.arch }}

      - name: Record verified package provenance
        shell: bash
        run: node scripts/packaged-build-provenance.mjs

      - name: Name verified package artifact
        id: artifact
        shell: bash
        run: >-
          echo "name=KYUTXO-${{ steps.version.outputs.version }}-${{ matrix.platform }}-${{ matrix.arch }}-${{ github.run_id }}"
          >> "$GITHUB_OUTPUT"

      - name: Upload verified package
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.artifact.outputs.name }}
          path: |
            release/*.exe
            release/verified-package-provenance.json
            release/win-unpacked/**
            dist/public/**
          if-no-files-found: error
