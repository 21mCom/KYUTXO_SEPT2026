node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# User data and attachments (should not be in repo)
data/attachments/
attached_assets/
# Demo vault builder HTTP cache (real blockchain data, resumable)
scripts/demo-vault/cache/

# Transient canary written/deleted by scripts/check-db-noise-guard.js — never commit it.
client/src/test/__dbNoiseGuardCanary__.test.ts
release
